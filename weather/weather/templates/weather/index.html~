{% if latest_poll_list %}
    <ul>
    {% for poll in latest_poll_list %}
        <li><a href="/weather/{{ node.id }}/">{{ node.Name }}</a></li>
    {% endfor %}
    </ul>
{% else %}
    <p>No nodes are available.</p>
{% endif %}
